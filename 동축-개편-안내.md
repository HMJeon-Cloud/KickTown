# 동네 축구왕 2.0 — 관리자 전용 개편 안내

## 무엇이 바뀌었나

| 항목 | 변경 |
|---|---|
| 로그인 | 이메일·카카오 로그인, 회원가입, 게스트 모드 **전부 제거** → 접속 시 **PIN 한 번** 입력 |
| 팀 탭 | 제거. **FC JEMIRO 고정** (부팅 시 자동 선택) |
| 팀원 탭 | 카카오톡 초대·링크 복사·계정 연결·통합 제거. 하단에 **팀 정보 수정** 추가 |
| 출석 탭 | GPS 체크인·출석 체크 목록 제거 → **경기장 지도 + 구글지도·TMAP·카카오내비·카카오맵·네이버지도 + 공유/링크복사**. 출석 점수 설정(지각·불참)은 유지 |
| 출석 입력 | 기록 탭에서 경기 탭 → 결과 입력 시 선수별 참석·지각·불참·미참석 지정 (기존과 동일) |
| 메뉴 탭 | 제거 (권한 관리·알림·비밀번호·로그아웃·회원탈퇴·엑셀 가져오기·투표·게시판 포함) |
| MOM | 투표 기능 제거에 따라 MOM 순위·시즌 MOM왕도 제거 |
| 사용 설명서 | 제거 |
| 기타 | FCM 푸시, iOS 네이티브 브리지(Capacitor), 카카오 OAuth 백엔드 연동 코드 제거 |

파일 기준 **6,744줄 → 약 3,200줄**, 삭제된 파일: `api/`(카카오 OAuth), `ios-build/`, `firebase-messaging-sw.js`, `privacy.html`, `delete-account.html` 등.

## 첫 접속 (PIN)

1. 배포 후 kick-town.com 접속 → **PIN 설정 화면**이 뜸 (4자리 이상)
2. 설정한 PIN의 해시는 Firestore `config/app.adminPinHash`에 저장 (코드·저장소에는 남지 않음)
3. 한 번 통과한 기기는 다시 묻지 않음 (localStorage 기억)

**PIN을 잊거나 바꾸고 싶으면**: Firebase 콘솔 → Firestore → `config` → `app` 문서에서 `adminPinHash` 필드 삭제 → 다음 접속 때 다시 설정.

※ PIN은 가벼운 잠금입니다. Firestore 규칙 자체는 기존과 동일하니, 주소를 함부로 공유하지 않는 것이 기본 보호입니다.

## FC AUSTINE 데이터 삭제 (1회)

1. **새 버전을 먼저 배포**하고 kick-town.com에 PIN 입장까지 진행
2. PC 크롬에서 `F12` → **Console** 탭
3. 함께 보낸 `cleanup-austine.js` 내용 전체를 붙여넣고 Enter
4. `✅ 정리 완료!` 로그가 나오면 끝 → 새로고침

스크립트가 하는 일: FC AUSTINE 팀 + 소속 팀원·경기·스쿼드·기록·출석 삭제, 그리고 이제 안 쓰는 컬렉션(`owners`, `pushTokens`, `polls`, `votes`, `posts`, `extraRequests`, `joinRequests`, `matchRoster`, `noteComments`) 전체 삭제. **FC JEMIRO 데이터는 건드리지 않습니다.**

## 이제 정리해도 되는 외부 서비스

- **Vercel** `kick-town.vercel.app` (카카오 OAuth 백엔드) → 프로젝트 삭제 가능
- **Vercel** `kicktown-push.vercel.app` (푸시 알림 백엔드) → 프로젝트 삭제 가능
- **카카오 개발자 콘솔**: 카카오 *로그인* 기능은 꺼도 됨. 단, **카카오맵 JS 키는 계속 사용 중**이니 앱 자체는 삭제하지 말 것
- **Firebase Authentication**: 이메일/비밀번호 제공업체는 꺼도 됨. **익명 로그인은 반드시 켜둘 것** (Firestore 접근에 사용)

## 무료 사용량

- 팀원 접속이 사라져 읽기량이 관리자 1인분으로 감소
- 팀 데이터도 명단·경기·스쿼드만 부팅 로드 (투표·게시판·명단 로드 제거)
- 기존의 화면별 지연 로드(기록·스쿼드 상세)는 그대로 유지
